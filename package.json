{
  "name": "maccies-consulting-site",
  "version": "1.0.0",
  "private": true,
  "description": "Maxwell Fleming personal link hub — built with 11ty",
  "scripts": {
    "dev": "eleventy --serve --port=${PORT:-5678}",
    "build": "eleventy",
    "start": "eleventy --serve --port=${PORT:-5678}"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
